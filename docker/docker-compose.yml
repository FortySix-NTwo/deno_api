version: '3'
services:
  db-container:
    image: postgres:12
    env_file:
      - '../.env.docker'
    ports:
      - '5442:5432'
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
